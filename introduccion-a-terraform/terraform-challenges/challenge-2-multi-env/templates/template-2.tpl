============
ENVIRONMENT
============
=====================================================
Nombre del environment: ${env_name}
    - Region: ${config.region}
    - Numero de nodos: ${config.instance_count}
    - Tipo de instancia: ${config.instance_type}
    - Tamanyo de la base: ${config.database_size}
    - Backup operativo: ${config.backup_enabled}
=====================================================